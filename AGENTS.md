- 使用 shadcn-ui 组件和变量，非必要不要自定义
- 每次回复的时候，必须开头加上："========== dai-shi.cn ============"
- 所有的文案必须用中文
- 所有的路由都用拼音（包括页面路径与页面内的锚点 ID）
- 不使用大量 CARD 风格
- 不可点击的元素不要添加 hover 效果或其他会暗示可交互的视觉反馈
- 所有与咨询、预约、电话相关的按钮或链接，必须使用 PhoneButton 或 PhoneLink 组件
- PhoneButton 和 PhoneLink 组件会自动使用配置在 site.ts 中的 SITE_HOTLINE_TEL 作为电话链接
- 二级页面顶部返回条统一使用网站通用的 `PageTopNav` 组件，不再使用页面或模块专属命名的顶部导航组件
- 二级页面（除首页外的所有页面）一律全屏布局：宽度由 `container mx-auto px-4` 控制，**内容层（卡片 / 章节 / CTA / 正文等）不要再加 `max-w-Xxl` 二次约束**，与首页 section 视觉保持一致；如需在视觉上居中，使用 `text-center` 或 `mx-auto` 配合具体场景
- 后续如果新增、删除或调整可访问页面路由，必须同步更新 `llms.txt` 与 `sitemap.xml`，确保站点清单与实际页面一致
- 后续所有“文案型详情页 / 专题型详情页”（如服务流程、家长指南、专题说明、招生介绍、长文案详情）统一参考 `/zhao-sheng-jian-zhang/2027` 的设计风格
- 这类页面的整体气质要偏“正式专题页 / 招生简章页”，不是博客页，也不是卡片列表页
- 首屏优先采用“大标题 + 导语 + 少量关键数据/目录摘要 + 明确 CTA”的结构，强调信息权威感、正式感、完整感
- 正文优先采用“左侧目录 / 侧边锚点 + 右侧主体内容”的长页结构；如果页面较短，也要保留清晰的目录感和章节秩序
- 章节标题统一做强层级设计，参考 `/zhao-sheng-jian-zhang/2027` 中的 `SectionHeading` 风格：有章节标签、序号感、标题、说明文，不要只丢一个普通小标题
- 内容排版以留白、分组、浅底分区、边框分隔、图文穿插来建立节奏，禁止把整页做成连续卡片墙
- 可以少量使用卡片承载“资质点 / 环境点 / 承诺点 / 数据点”，但只能作为局部辅助模块，不能成为页面主体结构
- 允许使用浅灰背景分区、深色首屏 Hero、图片占位、数据指标、认证说明、分栏信息块，这些都优先参考 `/zhao-sheng-jian-zhang/2027` 的节奏与比例
- 文案型详情页中的 CTA 位置优先放在首屏和收尾，正文中如需插入 CTA，数量要少，避免频繁打断阅读
- 文案型详情页的阅读节奏要“大段落少而稳、章节边界清楚、视觉重心明确”，避免做成碎片化资讯流或 FAQ 堆砌页
